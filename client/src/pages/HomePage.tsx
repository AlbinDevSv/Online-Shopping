const HomePage = () => {
    return (
        <main>
            <button style={{ width: "200px", height: "100px" }}>
                Checkout
            </button>
        </main>
    );
};

export default HomePage;
